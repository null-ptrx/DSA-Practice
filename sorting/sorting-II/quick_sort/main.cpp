#include <bits/stdc++.h>
using namespace std;

void quickSort (int arr[], int low, int high) {

}

int correct(int arr[], int low, int high) {
    int pivot = arr[low];
    int i = low;
    int j = high;

    while (i < j) {
        while (i < j && i < high) {
            if (i < pivot){
                pivot++;
            }
        }
    }
    
}
int main() {
    int     

    return 0;
}
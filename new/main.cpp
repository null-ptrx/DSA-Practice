#include <bits/stdc++.h>
using namespace std;

int main() {
    int x;

    // Read until end of file (important for file input)
    while (cin >> x) {
        cout << "Square of " << x << " = " << x * x << endl;
    }

    return 0;
}